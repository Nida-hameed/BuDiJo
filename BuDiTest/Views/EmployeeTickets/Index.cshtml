@*@model IEnumerable<BuDiTest.Models.EmployeeTicket>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RaisedByEmployeeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RaisedByEmployeeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ticket.TicketID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmployeeTicketID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmployeeTicketID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmployeeTicketID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@